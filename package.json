{
  "name": "blocktrace",
  "scripts": {
    "client:start": "npm run start --prefix client",
    "server:run": "npm run start:dev --prefix server",
    "run": "concurrently \"npm run server:run\" \"npm run client:start\""
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/boeschj/blocktrace.git"
  },
  "bugs": {
    "url": "https://github.com/boeschj/blocktrace/issues"
  },
  "dependencies": {
    "concurrently": "^7.4.0"
  }
}
